from setuptools import setup

setup(name='uploadsdemo',
      version='0.1',
      description='Gaussian distributions package for pypi upload',
      packages=['distributions'],
      zip_safe=False)
