from setuptools import setup

setup(name='Distributions_Normal_and_Binomial',
      version='0.1',
      description='Gaussian distributions and Binomial distributions',
      packages=['Distributions_Normal_and_Binomial'],
      zip_safe=False)
