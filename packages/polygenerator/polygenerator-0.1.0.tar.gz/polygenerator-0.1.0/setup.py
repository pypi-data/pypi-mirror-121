#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup


setup(name='polygenerator',
      version='0.1.0',
      description='Generates random polygons.',
      author='Radovan Bast',
      author_email='radovan.bast@uit.no',
      url='https://github.com/bast/polygenerator',
      py_modules=['polygenerator'],
     )
